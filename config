# codex_review config
#
# - 이 파일은 codex_review 설정 파일입니다.
# - 기본 경로: ~/.config/codex_review/config
# - 로컬에서 실행 → 원격으로 scp 전송 / spec pull 하는 흐름을 기본으로 둡니다.

# ===== Remote target =====
# send_review / pull_spec에서 사용할 SSH 접속 정보
CODEX_REVIEW_TARGET_USER=""
CODEX_REVIEW_TARGET_HOST=""

# send_review가 review 파일을 업로드할 디렉토리(원격)
CODEX_REVIEW_TARGET_DIR="~/review_diffs"

# pull_spec가 가져올 spec.md의 원격 경로(원격)
# - 보통 x220 웹에서 만든 spec.md를 이 경로에 저장해둔다고 가정
# - 비워두면 default로 CODEX_REVIEW_TARGET_DIR/spec.md 사용
CODEX_REVIEW_SPEC_REMOTE="~/review_diffs/spec.md"

# ===== Local project =====
# spec.md를 로컬에서 항상 "프로젝트 루트"로 넣기 위해 절대경로로 고정
CODEX_REVIEW_PROJECT_DIR=""

# pull_spec가 덮어쓸 로컬 spec 경로(로컬)
# - 원격 spec을 받아오면 무조건 이 경로를 overwrite 합니다.
CODEX_REVIEW_SPEC_LOCAL="$CODEX_REVIEW_PROJECT_DIR/spec.md"

# ===== Local review output =====
# make_review가 생성할 review 파일 저장 위치(로컬)
# - /tmp에 두면 repo가 더러워지지 않고, 파일이 커져도 부담이 적음
# - 필요하면 ~/review_diffs_local 같은 곳으로 바꿔도 됩니다.
CODEX_REVIEW_OUT_DIR="/tmp"
